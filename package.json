{
  "name": "egghead",
  "version": "1.0.0",
  "description": "## Lessons",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../server && npm install",
    "start": "concurrently \"cd frontend && npm start\" \"cd server && node app.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.4.0"
  }
}
