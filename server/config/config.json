{
  "development": {
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_DATABASE}",
    "dialect": "${DB_DIALECT}",
    "logging": true,
    "logQueryParameters": true
  },
  "test": {
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_DATABASE}",
    "dialect": "${DB_DIALECT}"
  },
  "production": {
    "host": "${DB_HOST}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_DATABASE}",
    "dialect": "${DB_DIALECT}"
  }
}
